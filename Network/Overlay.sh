    #cria uma nova rede overlay
    docker network create
    #lista as redes overlay disponíveis.
    docker network ls: 
    #exibe informações detalhadas sobre uma rede overlay.
    docker network inspect
    #conecta um contêiner a uma rede overlay.
    docker network connect 
    #desconecta um contêiner de uma rede overlay.
    docker network disconnect 
    #remove uma rede overlay.
    docker network rm 
