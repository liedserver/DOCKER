    #define o limite de memória em bytes que um contêiner pode usar.
    docker run --memory 
    #define o limite total de memória e troca (swap) que um contêiner pode usar.
    docker run --memory-swap 
    
    ##############
    docker stats
    
    ##############
    docker update
    
