    #define o número de "shares" de CPU que um contêiner pode usar. Quanto maior o número de shares, mais CPU o contêiner pode usar.
    docker run --cpu-shares 
    #define o limite de processos que um contêiner pode criar.
    docker run --pids-limit 
    
    #############
    docker stats
    
    ############
    docker update
    
